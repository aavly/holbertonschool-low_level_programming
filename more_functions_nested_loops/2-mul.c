#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: first digit to multiply.
 * @b: second digit to multipy.
 *
 * Return: returns product of the two digits.
 */

int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
